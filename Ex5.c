#include <stdio.h>
#include <math.h>

int n;

int main() {
	scanf("%c", &n);
	if (n == 'e' || n == 'u' || n == 'o' || n == 'a' || n == 'i') printf("YES");
	else printf("NO"); 
	return 0;
}
